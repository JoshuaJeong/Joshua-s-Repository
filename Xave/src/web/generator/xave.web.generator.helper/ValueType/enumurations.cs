using System.ComponentModel;

namespace xave.web.generator.helper.ValueType
{
    [DefaultValue(Value)]
    public enum DocumentGetterType
    {
        Code,
        CodeName,
        Value,
        ValueType
    }
}
